# Bubble Sort Procedure
def Bubble_Sort_Inefficient(Array):
    for index in range(0, len(Array)):
        for check in range(index, len(Array)):
            if Array[index] > Array[check]:
                temp = Array[index]
                Array[index] = Array[check]
                Array[check] = temp


# Check whether to sort integer or string
def Array_Input(Array):
    print("OPTION 1 : Sort Array for String")
    print("OPTION 2 : Sort Array for Integer")
    print("")
    choice = input("Enter Option Number: ")
    while choice != "1" and choice != "2":
        choice = input("Enter a valid option number: ")
    print("")
    print("DATA ENTRY")
    print("Enter 'done' when complete")
    print("")
    if choice == "1":
        x = input("Enter a string: ")
        while x != "done":
            Array.append(x)
            x = input("Enter a string: ")
    else:
        x = input("Enter a integer: ")
        while x != "done":
            try:
                x = int(x)
                Array.append(x)
            except:
                print("ERROR")
            x = input("Enter a integer: ")

# Produce Output
def Array_Output(Array):
    for i in range(len(Array)):
        if i == len(Array) - 1 :
            print("," , Array[i], ")")
        elif i == 0:
            print("(", Array[i], end="")
        else:
            print(",", Array[i], end="")


# Menu
Array1 = []
Array_Input(Array1)
print("")
print("Array before sorting: ")
Array_Output(Array1)
Bubble_Sort_Inefficient(Array1)
print("Array after sorting: ")
Array_Output(Array1)
